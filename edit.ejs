<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Book - <%= book.title %></title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>


<form  class="book-form" action="/edit/<%= book.id %>" method="POST">
    <h2>Edit Book</h2>
    <label for="title">Title</label>
    <input name="title" value="<%= book.title %>" required>

    <label for="author">Author</label>
    <input name="author" value="<%= book.author %>" required>

    <label for="date_read">Date Read</label>
    <input name="date_read" type="date" value="<%= book.date_read.toISOString().split('T')[0] %>" required>

    <label for="rating">Rating</label>
    <input name="number" value="<%= book.rating %>" min="1" max="10" required>

    <label for="notes">Notes</label>
    <textarea name="notes"><%= book.notes %></textarea>

    <label for="cover_url">Cover Image URL</label>
    <input name="cover_url" value="<%= book.cover_url %>">

    <button type="submit">Update Book</button>
</form>


<a href="/">Back to Book Lists</a>